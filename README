dbus

* 1.14.8 (2023-08-30):

    tools/dbus-launch-x11.c

    -  98  #define DBUS_DIR ".dbus"
    +  98  #define DBUS_DIR ".cache/dbus"


nsxiv

* 31-29-gd19924f (2023-09-08):

    main.c
            * add surrounding brackets to thumb/image mode file count
            * remove zoom level status

    -  459 bar_put(r, "%s%0*d/%d", mark, fw, fileidx + 1, filecnt);
    +  459 bar_put(r, "%s[%0*d/%d]", mark, fw, fileidx + 1, filecnt);

    -  476 bar_put(r, "%3d%%" BAR_SEP, (int)(img.zoom * 100.0));

    -  482 bar_put(r, "%0*d/%d", fw, fileidx + 1, filecnt);
    +  482 bar_put(r, "[%0*d/%d]", fw, fileidx + 1, filecnt);

    config.def.h
                  * update default geometry
                  * set statusbar to top
                  * change thumb size options

    -  4   static const int WIN_WIDTH  = 800;
    -  4   static const int WIN_HEIGHT = 600;
    +  4   static const int WIN_WIDTH  = 420;
    +  4   static const int WIN_HEIGHT = 460;

    -  19  static const bool TOP_STATUSBAR = false;
    +  19  static const bool TOP_STATUSBAR = true;

    -  77  static const int thumb_sizes[] = { 32, 64, 96, 128, 160 };
    +  77  static const int thumb_sizes[] = { 64, 112, 170, 354};

    -  80  static const int THUMB_SIZE = 3;
    +  80  static const int THUMB_SIZE = 2;


    window.c
              * increase statusbar padding

    -  47  H_TEXT_PAD = 5,
    -  47  V_TEXT_PAD = 1
    +  47  H_TEXT_PAD = 15,
    +  47  V_TEXT_PAD = 12

    thumbs.c
              <see patches dir for more info>
